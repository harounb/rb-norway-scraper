#!/usr/bin/env node

"use strict";

const scrape = require("../lib/scrape");
const ttt2 = require("../config/ttt2");

const args = process.argv;

if (args[2] === "ttt2") {
  scrape(ttt2);
} else {
  scrape({});
}
